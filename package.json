{
  "name": "ssh-conf",
  "version": "1.0.0",
  "description": "GUI for SSH config",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^8.0.1"
  }
}
